This artifact is only a container for the native library (stub artifact).
The actual, platform specific, native libraries are contained in the JAR files with the respective classifier (for example, "-windows-x86_64" or "-macos-x86_64").
